type iSchedule = {
    date: Date,
    name: string,
    timeSetting: string,
    timeRemaining: number,
    isChecked: boolean,
    timerIcon: string,
    checkIcon: string
}

export type {iSchedule}